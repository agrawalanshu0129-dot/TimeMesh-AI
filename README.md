# TimeMesh-AI
Smart shared calendar &amp; family coordination platform powered by Claude AI

## Free deployment targets

- **GitHub Pages**: existing workflow deploys on push to `main`
- **Netlify**: uses `netlify.toml` (`npm run build -- --base=/`, publish `dist`)
- **Render**: uses `render.yaml` static service (`npm run build -- --base=/`, publish `dist`)
