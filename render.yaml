services:
  - type: web
    name: timemesh-ai
    runtime: static
    branch: main
    autoDeploy: true
    buildCommand: npm install && npm run build -- --base=/
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
